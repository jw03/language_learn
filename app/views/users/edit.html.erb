<h1>Editing User</h1>

<%= form_for @user do |form| %>
  <%= render partial: 'form', object: form %>

  <div class="submit-field">
    <%= form.submit %>
  </div>

  <div class="other-links">
    <%= link_to t(".sign_in"), sign_in_path %>
  </div>

  <buttton class="btn btn-facebook">
    <b <i class="fa fa-facebook"></i> <%= link_to "| Signup with Facebook", '/auth/facebook' %> </b>
  </buttton>

<% end %>

<%= link_to 'Show', @user %> |
<%= link_to 'Back', users_path %>
